import { Layout } from 'antd';
import styled from 'styled-components';

const LayoutsWrapper = styled(Layout)`
  height: 100vh;

  .main-content {
    padding: 20px;
  }
`;

export default LayoutsWrapper;
